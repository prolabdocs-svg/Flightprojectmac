# Visual State

- Iteration: 8
- Current bottleneck: the first-flight scene is coherent and readable, but later-region prop density/material variety still need device-profiled capture review before an art-complete claim.
- Completed this iteration: the Blender-authored ultralight hero GLB is exported, loaded in the flight scene with its Blender-to-Three forward axis corrected, inherits the selected fabric/tube livery, animates its named propeller, and has cockpit/structural contrast and safety wingtips; flight cleanup is instance-safe; each runway uses the active airfield's dimensions, surface and graded elevation; the portrait camera uses a centered wider chase framing; hangar upgrade rows reflow on narrow screens; every campaign region streams sparse authored anchors; the map presents actual airfield positions rather than only a card list. Precision-landing zones now combine a ground ring with four pulsing vertical beacons, and the horizon has a low-cost solar focal point to improve aerial orientation without a skybox. Backcountry and coastal routes now render their existing simulation water footprints as subtle, animated, shared-elevation surfaces. Each footprint includes a slow, translucent shoreline band so its playable water boundary reads naturally from the chase view while retaining the exact terrain and collision plane. The coastal route has an emissive lighthouse landmark; industrial, desert, canyon, forest and range routes now have their own static skyline hito (stacks, radio mast, rock gateway, or lookout). These improve airborne orientation without adding dynamic lights or simulation cost. A data-backed visual-identity contract now assigns every terrain family a landmark and prevents later regions from silently inheriting rural art direction.
- Validation: local gameplay flight is stable through takeoff controls with no console errors in fresh desktop capture; the precision-mission capture confirms the authored airframe, graded strip, HUD bearing and destination distance. `npm run build` and 315 automated tests pass. Low-power mobile GPUs use a 1.25 DPR / 512px shadow tier.
- Production build: passes. Asset streaming retains procedural fallbacks when a GLB cannot load.
- Score and validation streak: the art direction is now cohesive enough for a closed beta visual review, not yet a public-sale art-complete claim. The remaining gate is a representative capture/profile set across all eight regions on target mobile hardware.
