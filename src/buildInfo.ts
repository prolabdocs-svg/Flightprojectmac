/** Injected by Vite from package.json so UI, saves and desktop artifacts identify one build. */
declare const __PROJECT_FLIGHT_VERSION__: string;

export const GAME_VERSION = __PROJECT_FLIGHT_VERSION__;
